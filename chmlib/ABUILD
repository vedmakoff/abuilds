# ABUILD generated by mkpkg_generator.sh

pkgname=chmlib
pkgver=0.40
pkgbuild=2
arch=("auto")

shortdesc=("chmlib (library for accessing CHM files)")
longdesc=("chmlib is a small library designed for accessing MS ITSS files. The ITSS file format is used for Microsoft Html Help files (.chm), which have been the predominant medium for software documentation from Microsoft during the past several years, having superceded the previously used .hlp file format. Homepage: http://www.jedrea.com/chmlib/")

tags=("dev-libs libs")

source=("http://morte.jedrea.com/~jedwin/projects/${pkgname}/${pkgname}-${pkgver}.tar.bz2")

build_deps="glibc-solibs"


BUILD_SYSTEM="autotools"
BUILD_KEYS="--prefix=/usr --libdir=/usr/lib$LIBDIRSUFFIX --mandir=/usr/man --sysconfdir=/etc"
INSTALL_KEYS="DESTDIR=$pkgdir"
