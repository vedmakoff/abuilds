# ABUILD generated by mkpkg_generator.sh

pkgname=glib
pkgver=1.2.10
pkgbuild=4
arch=("auto")

shortdesc=("Common C routines used by Gtk+ and other libs (version 1.x)")
longdesc=("GLib is a library which includes support routines for C such as lists, trees, hashes, memory allocation, and many other things. GLib is required by nearly everything that uses GTK+ version 1.x.")
tags=("dev-libs libs")
source=("ftp://ftp.gtk.org/pub/gtk/v1.2/${pkgname}-${pkgver}.tar.gz")
build_deps="glibc-solibs"

BUILD_SYSTEM="autotools"
BUILD_KEYS="--prefix=/usr --libdir=/usr/lib$LIBDIRSUFFIX --mandir=/usr/man --sysconfdir=/etc"
INSTALL_KEYS="DESTDIR=$pkgdir"

