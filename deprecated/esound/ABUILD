# ABUILD generated by mkpkg_generator.sh

pkgname=esound
pkgver=0.2.41
pkgbuild=1
arch=("auto")

shortdesc=("Enlightened Sound Daemon")
longdesc=("EsounD, the Enlightened Sound Daemon, is a server process that mixes several audio streams for playback by a single audio device. For example, if you're listening to music on a CD and you receive a sound-related event from ICQ, the two applications won't have to jockey for the use of your sound card. Install esound if you'd like to let sound applications share your audio device.")
tags=("media-sound libs")
source=("http://ftp.gnome.org/pub/GNOME/sources/$pkgname/${pkgver%.*}/$pkgname-$pkgver.tar.bz2")
build_deps="audiofile glibc-solibs"

before_build() {
  go_src_dir
  sed -i 's/drain/drop/' audio_alsa09.c
}

SLKCFLAGS="${SLKCLAGS} -lm"
BUILD_SYSTEM="autotools"
BUILD_KEYS="--prefix=/usr --libdir=/usr/lib$LIBDIRSUFFIX --mandir=/usr/man --sysconfdir=/etc -with-audiofile --without-libwrap --enable-alsa --disable-artstest"
INSTALL_KEYS="DESTDIR=$pkgdir"

after_build() {
  go_src_dir
  install -Dm755 ${filedir}/esd.sh "$pkgdir/etc/rc.d/esd"
}