# ABUILD generated by mkpkg_generator.sh

pkgname=libidl
pkgver=0.8.14
pkgbuild=1
arch=("auto")

shortdesc=("A front-end for CORBA 2.2 IDL and Netscape's XPIDL")
longdesc=("libIDL is a library licensed under the GNU LGPL for creating trees of CORBA Interface Definition Language (IDL) files, which is a specification for defining portable interfaces. libIDL is used by SeaMonkey and the Mozilla Suite.")

tags=("dev-libs libs")

source=("http://ftp.gnome.org/pub/gnome/sources/libIDL/0.8/libIDL-${pkgver}.tar.bz2")

build_deps="glibc-solibs pkg-config"


BUILD_SYSTEM="autotools"
BUILD_KEYS="--prefix=/usr --libdir=/usr/lib$LIBDIRSUFFIX --mandir=/usr/man --sysconfdir=/etc"
INSTALL_KEYS="DESTDIR=$pkgdir"

