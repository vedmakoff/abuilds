pkgname=virtinst
pkgver=0.600.4
pkgbuild=1
arch=('auto')

shortdesc="Programs to create and clone virtual machines"
longdesc=("Virtinst is a set of commandline tools to create virtual machines using libvirt: virt-install: provision new virtual machines virt-clone: clone existing virtual machines virt-image: create virtual machines from an image description virt-convert: convert virtual machines between formats")
source=("http://virt-manager.et.redhat.com/download/sources/${pkgname}/${pkgname}-${pkgver}.tar.gz")
tags="app-emulation utils"
build_deps="libvirt urlgrabber libxml2"

conflicts="virt-manager"

BUILD_SYSTEM="python"
#MAKE_WORD="python setup.py build"
BUILD_KEYS=
