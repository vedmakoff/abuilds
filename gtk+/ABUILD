# ABUILD generated by mkpkg_generator.sh

pkgname=gtk+
pkgver=1.2.10
pkgbuild=6
arch=("auto")

shortdesc=("gtk+ (graphical user interface toolkit, version 1.x)")
longdesc=("GTK+, which stands for the Gimp ToolKit, is a library for creating graphical user interfaces for the X Window System. This library has been largely replaced by GTK+ version 2, but there are still some good programs that require it.")

tags=("libs x11-libs")
source=("ftp://ftp.gtk.org/pub/gtk/v1.2/gtk+-${pkgver}.tar.gz")

build_deps="glib glibc-solibs libX11 libXau libxcb libXdmcp libXext libXt libXi"

before_build() {
  go_src_dir
  if [ "${ARCH}" = "x86_64" ]; then
    rm config.guess config.sub
    ln -s /usr/share/libtool/config/config.guess config.guess
    ln -s /usr/share/libtool/config/config.sub config.sub
  fi
}

BUILD_SYSTEM="autotools"
BUILD_KEYS="--prefix=/usr --libdir=/usr/lib$LIBDIRSUFFIX --mandir=/usr/man --sysconfdir=/etc --with-xinput=xfree"
INSTALL_KEYS="DESTDIR=$pkgdir"
