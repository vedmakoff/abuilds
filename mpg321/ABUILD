# ABUILD generated by mkpkg_generator.sh

pkgname=mpg321
pkgver=0.3.2
pkgbuild=1
arch=("auto")

shortdesc=("A completely Free drop-in replacement for mpg123, for the use of frontends, shell scripts, etc.")
longdesc=("mpg321 is a free command-line mp3 player, which uses the mad audio decoding library. mpg321 is written to be a drop-in replacement for the mpg123 player for front-ends such as gqmpeg and programs which use mpg123 to decode mp3 files (like gtoaster, and other CD-recording software). mpg321 requires libao.so from the oggutils package.")

tags=("console media-sound")

source=("http://downloads.sourceforge.net/project/${pkgname}/${pkgname}/${pkgver}/${pkgname}_${pkgver}.orig.tar.gz")

build_deps="glibc-solibs libao libid3tag libmad"

BUILD_SYSTEM="autotools"
BUILD_KEYS="--prefix=/usr --libdir=/usr/lib$LIBDIRSUFFIX --mandir=/usr/man --sysconfdir=/etc"
INSTALL_KEYS="DESTDIR=$pkgdir"

