# ABUILD generated by mkpkg_generator.sh

pkgname=python-dbus
pkgver=1.2.0
pkgbuild=1
arch=("auto")

shortdesc=("dbus-python (Python bindings for dbus)")
longdesc=("This package includes python bindings for the D-Bus messagebus.")

tags=("libs dev-python")
source=("http://dbus.freedesktop.org/releases/${pkgname}/${pkgname}-${pkgver}.tar.gz")
build-deps="docutils dbus-glib python"
provides="dbus-python"

BUILD_SYSTEM="autotools"
BUILD_KEYS="--prefix=/usr --libdir=/usr/lib${LIBDIRSUFFIX}"
INSTALL_KEYS="DESTDIR=$pkgdir"

