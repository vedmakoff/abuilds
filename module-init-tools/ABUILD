# ABUILD generated by mkpkg_generator.sh

pkgname=module-init-tools
pkgver=3.16
pkgbuild=3
arch=("noarch")

shortdesc=("module-init-tools to kmod transition package")

tags=("virtual sys-base")
build_deps="kmod"
adddep="kmod"


build() {
	echo "Building virtual package $pkgname"
}
